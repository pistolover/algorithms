

状态模式允许对象在内部状态改变时动态改变它的行为，对象看起来好像修改了它的类。


允许一个对象基于内部状态而拥有不同的行为。


通常会导致设计中的类的数目大量增加。

可以看出，环境类Context的行为request()是委派给某一个具体状态类的。通过使用多态性原则，可以动态改变环境类Context的属性State的内容
使其从指向一个具体状态类变换到指向另一个具体状态类，从而使环境类的行为request()由不同的具体状态类来执行。